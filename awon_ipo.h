int ipow(int x, int n)
{
    int i,p=1;
    // Перевіряемо чи задоволняе умові
    for (i=1; i<=n; i++) 
    // Якщо так виконуємо дію * 
    // в іншому випадку додаемо 1 и перевіряемо умову знову
    p*=x;
    // Повертаємо результат
    return p;
}
